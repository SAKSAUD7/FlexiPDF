{
  "name": "flexipdf-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package.json to facilitate Render deployment.",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": "20.x"
  }
} 